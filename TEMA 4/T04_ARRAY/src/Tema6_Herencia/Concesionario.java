package Tema6_Herencia;

import java.util.ArrayList;
/*  VER LUEGO DONDE ESTÁ EL ERROR
public class Concesionario {
    private ArrayList<Coche> arrayL_Coche; // deportivo utilitario , tienen en comun que son de tipo Coche.. es x eso q se tipa asi
    private String matricula;


    public Concesionario() {
        this.arrayL_Coche = new ArrayList();
        this.matricula = matricula;

    }

    public void agregarCoche(String matricula) {  // se pone matricula x que es algo que tienen en comun todos los coches
        boolean existe = false;// se puede hacer con el foreach x q modifico afuera
        for (int i = 0; i < arrayL_Coche.size(); i++) {
            if (arrayL_Coche.get(i).getMatricula().equalsIgnoreCase(matricula)) {
                System.out.println("El coche ya existe");
                existe = true;
                break;
            }
        }
        arrayL_Coche.add(new Coche());
    }

    public void buscarCoche(String matricula){

        for (Coche busCoche :arrayL_Coche) {
            busCoche.getMatricula().equalsIgnoreCase(matricula); // el buscar coche poniendo busCoche haría lo mismo que el arrayL_Coche.get(i)

        }
    }

    public boolean existeCoche(String matricula){

        for (int i = 0; i < arrayL_Coche.size(); i++) {
            if (arrayL_Coche.get(i).getMatricula().equalsIgnoreCase(matricula)) {
                System.out.println("El coche ya existe");
             //   existe = true;
                break;
            }
      //  return ;

    }

    }


*/

















