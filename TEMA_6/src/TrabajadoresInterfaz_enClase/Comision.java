package TrabajadoresInterfaz_enClase;

public interface Comision {
    double votar ();// se puede poner asi o como aparece en la clase... pero como no se donde se vota,directamente cada clase votamos
    void  obtenerBeneficio(int beneficio);



}
